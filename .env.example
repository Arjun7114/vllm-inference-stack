# Copy this file to `.env` and adjust as needed. `.env` is gitignored.

# Which model backend to use:
#   mock  -> canned responses, no GPU, for local development
#   vllm  -> talk to a real vLLM OpenAI-compatible server
BACKEND=mock

# Used only when BACKEND=vllm -- where the vLLM server is reachable.
VLLM_BASE_URL=http://localhost:8000
MODEL_NAME=mistralai/Mistral-7B-Instruct-v0.3

# API key clients must present.
API_KEY=dev-key-change-me

# Guardrails: absolute path to a local clone of llm-guardrails-gateway.
# If set and importable, real input/output screening activates.
# Leave empty for passthrough (the app still runs; guardrails no-op).
# Example: GUARDRAILS_GATEWAY_PATH=C:/Users/you/llm-guardrails-gateway
GUARDRAILS_GATEWAY_PATH=
